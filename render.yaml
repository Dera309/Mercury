services:
  # API Backend
  - type: web
    name: mercury-api
    runtime: node
    plan: free
    buildCommand: pnpm install && pnpm --filter mercury-investment-platform-api build
    startCommand: pnpm --filter mercury-investment-platform-api start
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 3001
      - key: FRONTEND_URL
        sync: false # Should be the URL of the mercury-web service
      - key: MONGODB_URI
        sync: false
      - key: JWT_SECRET
        generateValue: true
      - key: JWT_EXPIRES_IN
        value: 7d

  # Web Frontend (Next.js SSR)
  - type: web
    name: mercury-web
    runtime: node
    plan: free
    buildCommand: pnpm install && pnpm --filter mercury-investment-platform-web build
    startCommand: pnpm --filter mercury-investment-platform-web start
    envVars:
      - key: NODE_ENV
        value: production
      - key: NEXT_PUBLIC_API_URL
        sync: false # Needs to point to the mercury-api URL
