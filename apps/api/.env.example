# Mercury Investment Platform API - Environment Variables
# Copy this file to .env and fill in your values

# Database Configuration
# Get your MongoDB URI from MongoDB Atlas (free tier available at https://www.mongodb.com/atlas)
# For Render deployment, use your MongoDB Atlas connection string
MONGODB_URI=mongodb+srv://<username>:<password>@cluster<id>.mongodb.net/mercury-investment?retryWrites=true&w=majority

# JWT Configuration
# IMPORTANT: Generate a strong secret using: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
# Render will auto-generate this if you use generateValue: true in render.yaml
JWT_SECRET=your-generated-secret-key-here
JWT_EXPIRES_IN=7d

# Server Configuration
PORT=5000
NODE_ENV=production

# CORS Configuration (optional)
# Set this to your Render web service URL in production
FRONTEND_URL=https://mercury-web.onrender.com

# Bcrypt Configuration
BCRYPTJS_SALT_ROUNDS=10
